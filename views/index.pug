extends layout

block header
    div.navbar-fixed
        nav#header
            div.nav-wrapper
                a.brand-logo(href="#") eManual
                ul.right
                    li
                        a.button.alt(href='/login') Log in


block stylesheet
    link(rel="stylesheet" href="assets/css/index.css")