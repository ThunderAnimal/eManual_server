extends layout

block header
    div.navbar-fixed
        nav#header
            div.nav-wrapper
                a.homepage-link(href='#')
                    img.logo-pic(src='/assets/img/logo.png', alt='dash')

block main
    .container
        h1= message
        h2= error.status
        pre #{error.stack}